name: Android NDK Build

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build NDK
        run: $ANDROID_NDK_HOME/ndk-build -j4 -B NDK_PROJECT_PATH=. NDK_APPLICATION_MK=./Application.mk
